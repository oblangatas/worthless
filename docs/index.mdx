---
title: "Make API keys worthless to steal"
description: "Split-key proxy with hard spend caps. Your leaked .env becomes worthless."
template: splash
hero:
  title: Make API keys worthless to steal.
  tagline: Split-key proxy with hard spend caps. Leaked .env? Useless.
  actions:
    - text: Add to Cursor
      link: cursor://anysphere.cursor-deeplink/mcp/install?name=worthless&config=eyJjb21tYW5kIjoibnB4IiwiYXJncyI6WyIteSIsIndvcnRobGVzcy1tY3AiXX0=
      icon: right-arrow
      variant: primary
    - text: Other tools & CLI
      link: "#install"
      variant: minimal
    - text: GitHub
      link: https://github.com/oblangatas/worthless
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## Install

Pick your tool. The AI-tool paths need only Node — **no Python**.

<CardGrid>
  <Card title="Cursor" icon="seti:code-search">
    **One click:** [Add to Cursor](cursor://anysphere.cursor-deeplink/mcp/install?name=worthless&config=eyJjb21tYW5kIjoibnB4IiwiYXJncyI6WyIteSIsIndvcnRobGVzcy1tY3AiXX0=) — mounts 4 tools. [What's this? →](/install-mcp/)
  </Card>
  <Card title="VS Code" icon="seti:code-search">
    **One click:** [Install in VS Code](vscode:mcp/install?%7B%22name%22%3A%22worthless%22%2C%22command%22%3A%22npx%22%2C%22args%22%3A%5B%22-y%22%2C%22worthless-mcp%22%5D%7D).
  </Card>
  <Card title="Claude Code" icon="rocket">
    **One command:**

    ```bash
    claude mcp add worthless -- npx -y worthless-mcp
    ```
  </Card>
  <Card title="Claude Desktop" icon="laptop">
    Add the `npx worthless-mcp` block to your MCP config — one-click bundle coming. [Steps →](/install-mcp/)
  </Card>
  <Card title="No AI tool — just the CLI" icon="laptop">
    No Python needed:

    ```bash
    curl -sSL https://worthless.sh | sh
    ```
  </Card>
  <Card title="Already have Python?" icon="seti:python">
    `pipx install worthless`. [Solo-dev guide →](/install-solo/)
  </Card>
</CardGrid>

Also: [Docker](/install-docker/) · [GitHub Actions](/install-github-actions/).

The one-click editor paths run `npx worthless-mcp`, whose 4-tool mount is proven in CI (WOR-809). Cursor & Claude Code additionally verified by hand on macOS; Windsurf **unverified**.

## How it works

Three invariants. No exceptions.

- **Client-side splitting.** Your key is split on your machine. The server only ever sees half.
- **Gate before reconstruction.** Budget exceeded? The key never forms. Zero provider calls.
- **Direct upstream.** The reconstructed key calls the LLM provider directly. It never returns to the proxy.

Read the [security model](/security/) and [wire protocol](/protocol/) for the full picture.
